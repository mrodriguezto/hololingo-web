import type { NextPage } from 'next';
import Layout from 'layout';

const ProfilePage: NextPage = () => {
  return (
    <Layout pageTitle="Perfil" pageDescription="Visualiza y edita tu perfil">
      Trabajanding...
    </Layout>
  );
};

export default ProfilePage;
