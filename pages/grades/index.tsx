import type { NextPage } from 'next';
import Layout from 'layout';

const GradesPage: NextPage = () => {
  return (
    <Layout
      pageTitle="Calificaciones"
      pageDescription="Lista de todas las calificaciones"
    >
      Trabajanding...
    </Layout>
  );
};

export default GradesPage;
