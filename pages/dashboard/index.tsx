import Layout from 'layout';
import type { NextPage } from 'next';

const DashboardPage: NextPage = () => {
  return (
    <Layout pageTitle="Dashboard" pageDescription="Página principal">
      DashboardPage
    </Layout>
  );
};

export default DashboardPage;
